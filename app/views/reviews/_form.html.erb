<%= render 'shared/errors', object: @review %>

<%= form_with model: @review do |f| %>
<div class="cont_review">
  <div class="mb-3 row">
    <div class="col-sm-2 col-form-label">
    </div>
    
    <div class="col-sm-10">
    <%= f.text_area :body, class: "form-control", required: true, minlenght: 5 %>
    </div>
  </div>

    <%= f.submit "Отправить", class: "btn btn-lg"%>
<% end %>
</div>
